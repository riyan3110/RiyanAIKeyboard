package com.riyan.aikeyboard

/*
 * PRIVATE legacy compatibility stub.
 *
 * This file is intentionally NOT an Activity and is NOT registered in AndroidManifest.xml.
 * The old standalone settings page has been removed from the app UI. These commented
 * markers are kept only so older build-time migration patches can remain idempotent
 * while the live settings UI is KeyboardSettingsOverlay / SettingsActivity.
 *
        root.addView(sectionTitle("xKiro"))
        val xKiroKey = secretField("API key xKiro", prefs.getString("xkiro_api_key", ""))

                    .putString("9router_model", nineRouterModel.text.toString().trim().ifBlank { "cc/claude-sonnet-4-20250514" })
                    .putString("xkiro_api_key",
 */
